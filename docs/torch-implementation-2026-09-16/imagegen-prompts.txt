Built-in image_gen used for both production masters. Generated files were RGB with a neutral exterior matte; production script removes only connected exterior pixels and exports alpha WebP.

HAND / TORCH
Use case: background-extraction / precise-object-edit. Input image is an approved game torch composition reference. Produce a PRODUCTION transparent-background cutout of ONLY the first-person LEFT hand, dark weathered brown leather glove, forearm sleeve and wooden torch from the lower left of this reference. Do not reproduce the screenshot or any interface or scenery. Portrait 2:3 canvas, high detail photorealistic dark-fantasy materials, strong chiaroscuro. The forearm enters from the bottom-left edge, gloved LEFT hand grips the wooden shaft near the lower middle. Shaft leans diagonally up and right about 25 degrees from vertical, its wrapped charred fuel head near x=74%, y=19% of canvas. Its top end near x=79%, y=11%. Lower wooden shaft ends beyond the bottom edge. The forearm/glove should look convincingly anatomical and naturally gripping. Preserve the approved reference's camera perspective and convincing old leather, rough wood and charred rope wrapping. Very modest warm rim light from the torch head on rope/glove, otherwise muted dark browns and brass matching reference. IMPORTANT: Remove ALL flame, glowing fire, embers, sparks, smoke and surrounding halo; the torch head must be dark charred cloth/rope, ready for a separately rendered animated flame. Genuine transparent alpha everywhere outside the hand/forearm/torch, not a black, white, gray or checkerboard background. No skulls, candles, stone plinth, ground, cards, UI, text, border or watermark. Do not crop the torch head or fingers. Static isolated cutout asset, no animation frames.

FOREGROUND CLEANUP
Use case: precise-object-edit / production UI foreground alpha cleanup. Edit target is an existing 1536x1024 transparent game foreground asset. Preserve exact canvas dimensions, alignment and transparent alpha. Remove the entire LEFT decorative cluster: the large skull, small skull, all candles/wax/flames, all raised stone pedestal steps and left vertical chain that are located in the left approx 0%-28% width. Those old objects must disappear fully, revealing genuine transparent pixels above the bottom foreground rim. The lower-left above bottom 90% of canvas should become empty transparent space for a separately rendered hand-held torch. At the very bottom only, finish a low irregular dark weathered stone/cloth rim that merges naturally into the existing center-bottom cloth. Preserve existing center-bottom black fabric with gold sigil, bottom chains, old parchment/map, compass on bottom right, and ALL of the right-edge tentacles exactly in their original positions, scale, proportions and photographic dark-fantasy rendering. Do not introduce a replacement hand, torch, flame, skull, pedestal or any new objects. Do not fill the transparent center with scenery. Keep genuine alpha everywhere except the remaining bottom rim and right edge ornaments. No text, no checkerboard painted into pixels, no new background, no recoloring, no rectangular matte. Only this left decoration removal/cleanup; everything from x=450px to right edge should remain visually unchanged.

