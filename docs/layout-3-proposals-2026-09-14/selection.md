# 3A 选定稿

日期：2026-09-14。用户本次明确选择“这批里的第一版”为主体，局部采用第三版的按钮亮度处理。本记录覆盖此前推荐以 v4 为最终图的结论；此前各轮图片与说明保留作对照。

| 用途 | 对应文件 | 使用范围 |
| --- | --- | --- |
| 本批第一版，主体依据 | [iterations/layout-3A-v2.png](iterations/layout-3A-v2.png) | 整体构图、组件位置与形状、场景和材质、四个按钮内部底图色调 |
| 本批第三版，局部参照 | [layout-3A-v4-weathered-contour.png](layout-3A-v4-weathered-contour.png) | 仅参照四个按钮边框及文字的较低亮度 |
| 本次合并效果图 | [layout-3A-selected-composite.png](layout-3A-selected-composite.png) | 展示上述组合与 SAN 刻度调整 |
| 本次生成提示词 | [layout-3A-selected-prompt.json](layout-3A-selected-prompt.json) | 记录内置 image_gen 的编辑要求与输入 |

本次只做以下细改：

1. 保留第一版的整体设计，不将第三版的布局或其他组件样式整体替换进来。
2. 四个按钮的边框和文字亮度降到第三版水平；按钮内部底图的原有色调不动，继续通过底色区分操作。此次亮度调整不扩展到整个按钮或整张画面。
3. 所有角色 SAN 条仍在 SAN=6 对应位置保留固定刻度线，去掉该阈值旁的数字或数字徽章。角色当前真实 SAN 数值继续显示；当前 SAN 恰好为 6 时，该真实数值也保留。

后续实装继续直接使用游戏内真实卡面、衍生牌和卡背，保持 392:590 的既定卡牌比例。生成图中的卡牌内容、文字、数值与进度条用于说明构图，运行时由现有素材和状态组件提供，不把这些动态内容切成位图。

当前仍为设计效果图阶段，本次没有修改游戏运行代码或接入新素材。
